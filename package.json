{
  "name": "bak-backend",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "bld": "bun build src/index.ts --compile --outfile server"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "hono": "^4.6.20",
    "random-words": "^2.0.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
